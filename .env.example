# Google OAuth 設定
# 從 Google Cloud Console 取得：https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT 設定
# 用於簽發自家的 Access Token，請使用隨機字串
# 可用指令產生：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-for-jwt

# JWT 演算法設定
# HS256: HMAC + SHA-256（對稱式，適合單一伺服器）
# RS256: RSA + SHA-256（非對稱式，適合微服務架構）
ALGORITHM=HS256

# Token 過期時間（分鐘）
ACCESS_TOKEN_EXPIRE_MINUTES=60
